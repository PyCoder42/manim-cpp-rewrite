# Migration Notes

`manim-cpp-migrate` is a dedicated conversion tool that targets deterministic
translation for supported Python scene constructs and explicit TODO markers for
unsupported dynamic patterns.
