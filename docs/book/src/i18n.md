# Internationalization

Locale catalog continuity is preserved from existing `docs/i18n` assets (`fr`, `hi`,
`pt`, `sv`). During migration, gettext artifacts remain source-of-truth until
C++ docs extraction pipeline is fully switched.
